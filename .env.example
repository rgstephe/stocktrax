# Copy to .env and adjust. These are read on first run to seed settings;
# after that, most are editable in the Admin → Settings screen.

PORT=3000
COMPANY_NAME=Ultra Pest Control

# Starter admin password for the console login (change immediately in-app).
# Only used the very first time the database is created.
ADMIN_PASSWORD=admin

# Barcode lookup: "upcitemdb" (general retail) or "openfoodfacts" (food).
BARCODE_PROVIDER=upcitemdb
BARCODE_API_KEY=

# Where the SQLite database file is stored.
DATA_DIR=./data

# Set to true only when serving StockTrax over HTTPS (adds Secure to the
# session cookie). Leave false for plain-HTTP LAN use.
COOKIE_SECURE=false
