{
  "name": "allokis",
  "version": "0.7.0",
  "description": "Self-hosted barcode inventory: scan it in, scan it out, every movement logged per person. Everything accounted for.",
  "license": "AGPL-3.0-or-later",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2"
  }
}
